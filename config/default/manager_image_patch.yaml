apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: docker.intuit.com/personal/eramirez8/keikoproj/addon-manager:24oct2023-1
        name: manager
